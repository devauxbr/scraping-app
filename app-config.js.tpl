/** This is a template file. Copy it to "app-config.js" and change settings according to your needs
 * before accessing index.html
 */

/** Default root url if your run "node app.js" on your local machine
 * You must change this url to your server Node app url if you run "node app.js" on a remote machine
 */
var ROOT_URL = "http://localhost:3000";

/**
 * If your machine is behind a corporate proxy you can configure it :
 */
requestProxy = {
    //proxy: 'http://your.proxy.org:8080'
};